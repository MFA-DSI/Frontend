// nprogress-config.js
import NProgress from "nprogress";
import "nprogress/nprogress.css"; // Importer le style par défaut

// Configuration optionnelle
NProgress.configure({ showSpinner: false }); // Désactiver le spinner (optionnel)

export default NProgress;
